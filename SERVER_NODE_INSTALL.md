# 服务器节点安装教程

这份说明给新的 Debian 服务器使用。路由器管理器只负责保存节点信息，不会把服务器安装脚本上传到路由器。

## 推荐协议

默认推荐使用 `VLESS Reality + XUDP`：

- 入口协议：VLESS Reality
- 端口：443
- UDP 承载：`packet-encoding: xudp`
- SNI：填写常见 HTTPS 域名，例如 `www.microsoft.com`

这个方案不依赖自动申请证书，适合 Debian 10/11/12，也更适合作为长期稳定节点。

## 安装前准备

你需要准备：

- 一台 Debian 10、11 或 12 服务器
- root 登录权限
- 一个域名，建议解析到服务器 IP

域名不是必须的，但建议使用域名。以后服务器换 IP 时，只改 DNS 解析即可，路由器里的节点地址不用反复修改。

## 安装方式

在服务器上安装 Xray，并创建 VLESS Reality 入站。安装完成后，把下面这些信息复制到管理页的 `自建节点`：

- 节点名称
- 协议：`VLESS Reality`
- 地址：服务器 IP 或域名
- 端口
- UUID
- SNI
- Flow
- Reality 公钥
- Reality Short ID

如果这台服务器本身就是最终 ISP 出口，在管理页勾选 `ISP 作为最终出口`。

如果这台服务器只是前置中转，不要勾选 `ISP 作为最终出口`。这样它会先作为前置节点，最后再走 `静态住宅IP` 当前选中的出口。

## 使用建议

- 优先用 TCP 443，表现更像普通 HTTPS。
- SNI 不要乱填，建议使用常见 HTTPS 站点域名。
- 如果服务器 IP 已被封，单纯更换域名通常没有用，因为域名最终还是解析到同一个 IP。
- 如果你需要多台服务器，建议每台都使用容易识别的节点名称，例如 `ISP出口-1`、`中转-洛杉矶-1`。

## 填入管理页

打开路由器管理页：

```text
http://192.168.31.1:19999/
```

进入 `自建节点`，新建节点并保存。保存后点顶部的 `重启 ShellCrash`，配置就会重新加载。

常见选择方式：

- 只用订阅节点：`自建节点` 选 `使用订阅节点`，`静态住宅IP` 选 `不使用静态住宅IP`
- 订阅节点后接住宅出口：`自建节点` 选 `使用订阅节点`，`静态住宅IP` 选具体住宅出口
- 直接使用 ISP 出口：`自建节点` 选 `直接使用静态住宅IP`，`静态住宅IP` 选对应 ISP 节点
- 自建中转后接住宅出口：`自建节点` 选中转节点，`静态住宅IP` 选具体住宅出口
